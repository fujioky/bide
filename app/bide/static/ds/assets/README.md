# assets/

**Empty on purpose.** No logo, icons, illustrations or photography were supplied with the Lyra Blog brief.

- The Lyra constellation SVG mentioned in the spec ("已有 SVG") was not attached — nothing was drawn in its place. `components/shell/Wordmark.jsx` sets **◆ Lyra** in type instead. Drop the real file here as `logo.svg` and render it inside that component.
- Icons come from Lucide via CDN (see ICONOGRAPHY in `readme.md`). If Lyra has its own set, add it here and rewrite `components/icons/LyraIcon.jsx`.
- Image slots in the UI kit render labelled placeholders; no imagery was generated.
