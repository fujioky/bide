One-line: editable code cell + 运行 button + output pane, split by a dashed rule; template D only.

```jsx
<Playground lang="Python" code={"import torch\nx = torch.randn(3)"} output="tensor([0.1234, -1.02, 0.55])" />
```
