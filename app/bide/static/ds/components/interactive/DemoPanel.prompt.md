One-line: the green-dotted container that tells readers "this one is live"; every simulator, slider set and canvas demo goes inside one.

```jsx
<DemoPanel title="噪声强度">
  <SliderControl label="σ" value={0.7} />
</DemoPanel>
```
The green dot is the only place Lyra uses a status colour decoratively.
