One-line: full-measure code figure — language label left, 复制 right, horizontal scroll, never wrapped lines.

```jsx
<CodeBlock lang="python" code={"def flow_matching(x0, x1, t):\n    return (1 - t) * x0 + t * x1"} />
```
In production, syntax highlighting is Prism/Shiki. Enters by sliding in from the left in the technical template.
