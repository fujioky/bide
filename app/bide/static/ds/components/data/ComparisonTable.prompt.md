One-line: the data template's workhorse table — sticky header, zebra rows, row hover, horizontal scroll on narrow screens.

```jsx
<ComparisonTable columns={["国家","月薪","租房","净剩"]} rows={[["瑞士","€4,200","€1,500","€2,700"]]} />
```
Numeric columns are right-aligned and tabular by default.
