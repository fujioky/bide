One-line: the always-present AI affordance — a slow 3s breathing pulse (scale 1 → 1.05) is the only ambient animation Lyra allows.

```jsx
<GrokLauncher onClick={open} />
<GrokLauncher label="Ask" />
```
