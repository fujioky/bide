One-line: the signed-in admin state of the header's far right — a 24px accent dot that opens a glass dropdown (管理面板 / 登出).

```jsx
<UserMenu name="杜嘉诚" open={open} onToggle={() => setOpen(!open)} />
```
Visitors see a ghost 登录 button in this slot instead.
