One-line: the Lyra lockup — wide-tracked mono type plus an accent ◆; stands in for the (not-yet-supplied) constellation SVG.

```jsx
<Wordmark size={16} />
```
Never re-draw the mark. If assets/logo.svg is added, render it here at the same optical size.
