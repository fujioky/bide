One-line: expanded TOC — 280px frosted panel sliding in from the left on desktop, half-height bottom sheet on mobile.

```jsx
<TocPanel sections={sections} activeIndex={2} onClose={close} />
```
Filled dot = current section, hollow dot = other, `level: 2` indents as a sub-section. Sits above content, below the Grok panel.
