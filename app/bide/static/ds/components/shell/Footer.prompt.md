One-line: a single quiet line closing the page — copyright left, RSS + 回顶部 right. Nothing else.

```jsx
<Footer />
<Footer mobile showBackToTop={false} />
```
Rules: no wordmark (the header owns it), no tagline, no social row, at most one icon (arrow-up). There is no GitHub link.
