One-line: the only button in the system — press feedback is a 0.97 scale, never a colour flash.

```jsx
<Button variant="solid" icon={<LyraIcon name="send" size={14} />}>发送</Button>
<Button variant="ghost" size="sm">登录</Button>
```

Variants: solid (accent fill), outline (hairline), ghost (nav), quiet (raised surface). Never place two solid buttons side by side.
